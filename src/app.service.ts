import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // TODO step 01: expose the data the AppController routes will return.
  // See docs/steps/01-bootstrap-modules.md.
}
